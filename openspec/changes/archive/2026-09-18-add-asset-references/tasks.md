## 1. Lexical rules (vault layer, no editor, no DOM)

- [x] 1.1 Add `linkDestinationTrigger(before, after)` to `src/vault/parse.ts` beside `referenceTrigger`: the last `](` before the caret with a matching `[` earlier in the textblock, no `)` immediately after the caret, a non-empty typed destination between them, `isVaultRelative(query)` true and the query not starting with `#`; returns the typed text and its length so the accept step can compute the replace range. Verify with unit tests in `src/vault/parse.test.ts` covering: a plain destination, an image destination, an empty destination, a closed destination, a scheme, an absolute path, a fragment, a destination inside a code span, and a `](` with no opening `[`.
- [x] 1.2 Add `markdownDestination(path)` beside `linkForAsset` in `src/components/dropAssets.ts`: percent-encode `%`, space, `(`, `)`, `<`, `>`, `"`, `'`, and backtick, and leave `/` and every other character alone. Verify with a table test in `src/components/EditorPane.test.tsx` (`assets/q3-report.pdf` unchanged, `assets/Q3 report.pdf` -> `assets/Q3%20report.pdf`, `assets/a (draft).pdf`, `assets/100% done.pdf`, and a non-ASCII name left readable), plus a decode round-trip assertion for each case.
- [x] 1.3 Make `linkForAsset` write through `markdownDestination` so drop and paste emit a readable destination. Verify by updating the existing `linkForAsset` expectations and the drop/paste insertion assertions (`[Q3 report](assets/Q3%20report.pdf)`), and by round-tripping every written form through the index's `parseAssetPaths` to confirm it still yields the file's literal path.
- [x] 1.4 Add the file candidate pool to `src/vault/suggest.ts`: `fileCandidates(graph)`, one row per vault path that is not a page, `name` = `assetName(path)`, plus the precomputed `lower`, `starts`, and `image` flag, path-ordered. Verify with unit tests that a file under `assets/` and a non-page file outside it are both pooled, that a page path and a hidden path are not, and that the row's `image` flag follows the same extension set `linkForAsset` uses.
- [x] 1.5 Narrow the ranker by the typed syntax: an image destination skips rows whose `image` flag is false, with no allocation per visited row. Verify with unit tests that `q3` under `![` yields nothing while `q3` under `[` yields `q3-report.pdf`, and that both kinds rank identically otherwise.

## 2. Completion (editor layer)

- [x] 2.1 Add `kind` (`page` | `file`) to `SuggestionState` in `src/editor/referenceSuggest.ts`; `derive` asks the destination trigger first, then the reference trigger, and each kind gets its own source and its own candidate narrowing. Verify with unit tests that a `#`-leading destination falls through to the page kind, that a page token inside a destination typed as `#[[` still completes, and that a keystroke touching neither document nor selection still returns the previous state object.
- [x] 2.2 Parameterise the adapter seam: `setSuggestionSource` gains the file source (a second query function, or a source object with both), and the popup's accessible name follows the kind. Verify with tests that the popup label is `Pages` for a page trigger and `Files` for a destination trigger.
- [x] 2.3 Branch `accept` for the file kind as one transaction that replaces the whole typed construct — the label, `](`, the destination, the trailing `)` if the caret left one, and the `!` in an image's `![` — with a text node carrying the link mark, or with an image node when the syntax is an image's. Label = the typed label, or the file's stem when empty; destination = `markdownDestination(path)`. Verify with tests for all four shapes (empty label, typed label, image, labelled image), for undo restoring the typed text in one step, and for the caret landing after the inserted construct with the popup closed.
- [x] 2.4 Verify in a real browser that an accepted link is a link: the text renders as a link, Ctrl+Click opens the file, the saved Markdown contains no escaped brackets, and reloading the page keeps the link. Use `npm run dev:test` (confirm the log says `ready in`), open a folder holding `assets/Q3 report.pdf`, and sweep with `npm run kill:dev` afterward.

## 3. Wiring

- [x] 3.1 Add the memoized file pool to `src/App.tsx` (`useMemo(() => fileCandidates(graph), [graph])`), mirroring `suggestPool`, and pass the file source to `EditorPane` through to the adapter's seam. Verify with the existing App tests that the pool rebuilds only when the graph identity changes and that the sidebar's memo still skips re-rendering on a keystroke.
- [x] 3.2 Confirm the keystroke budget with a measurement (AGENTS.md): with a vault of 10,000 pages and 10,000 assets, typing inside a link destination and typing in prose both hold the same per-keystroke cost as before this change, and the pool is built once per scan, not per keystroke. Record the numbers in `tasks.md` or the change's notes.

## 4. Verification and release

- [x] 4.1 Walk every scenario added to `specs/vault-assets` and `specs/page-editing` and confirm each one is either covered by a test or verified by hand; note any scenario left to the browser check. Every one is covered: the unit tests hold the trigger guards, the accept shapes, the escaping and its round-trip through the index, and the image narrowing; the browser check covered the typed prefix, the accept, a file already on disk being referenced again, the saved Markdown, the References row, the silent cases, the image form and Ctrl+Click on the completed link.
- [x] 4.2 Run `npx oxlint --fix`, `npm run fmt`, `npm test`, and `npm run build`; then `npx oxlint --deny-warnings --format=agent` clean.
- [x] 4.3 Bump `package.json` to 0.12.0 (new user-facing capability) and add the numbered task to `PLAN.md` describing what shipped.
