## 1. Chord parsing and the editor seam

- [x] 1.1 Add `src/editor/chord.ts` exporting `chordToKeyEventInit(chord)`, the inverse of `displayKeys`: split on `-`, the last part is the key, the rest are modifiers, `Mod` maps to `metaKey` on macOS and `ctrlKey` elsewhere, and the returned init always sets `bubbles: true` and `cancelable: true`. Verify: new unit tests cover `Mod-b`, `Shift-Mod-z`, `Mod-]`, `Shift-Tab`, `Enter`, `Backspace`, assert the platform mapping for both `Win32` and `MacIntel` (mirroring the `displayKeys` tests), and assert `cancelable` is true.
- [x] 1.2 Add `applyChord(chord: string): boolean` to `EditorAdapter` in `src/editor/editor.ts`, and implement it in `src/editor/fakeEditor.ts` recording each chord in a `readonly chords: string[]` array and returning true. Verify: `npx vitest run src/components/EditorPane.test.tsx src/editor/mount.test.tsx src/components/App.test.tsx` passes and the fake exposes applied chords to tests.
- [x] 1.3 Implement `applyChord` in `src/editor/milkdown.ts`: track the last `focusin` target inside the mount root, pick it as the dispatch target when it is still inside `view.dom` and otherwise fall back to `view.dom`, focus that surface when the editor lacks focus, dispatch `new KeyboardEvent('keydown', chordToKeyEventInit(chord))` at it, and return `event.defaultPrevented`. Verify: tests assert a bound chord is claimed (bold applied to a selection) and an unbound chord is not; the `focusin` listener is removed in `destroy`.

## 2. The reference becomes controls

- [x] 2.1 Update `src/components/shortcuts.ts`: add `replayable?: boolean` to `ShortcutItem` (default true), add `target: 'editor' | 'app'` to each group, split `Heading 1-6` into six entries (`Mod-Alt-1` … `Mod-Alt-6`) and mark `Paste as plain text` with `replayable: false`. Verify: `src/components/shortcuts.test.ts` passes with the drift guard rewritten to check each heading level's own row, and the `expandRange` helper and its range test are gone.
- [x] 2.2 Rework `src/components/ShortcutsList.tsx`: render one `<button>` per chord with the `<kbd>` inside and an accessible name combining the row label and the key tokens, keep the label as text, keep a bare `<kbd>` for a `replayable: false` row, call `onApply(chord, target)`, and disable rows whose surface is unavailable. Verify: `src/components/ShortcutsList.test.tsx` asserts one button per chord, the accessible names for a two-chord row, no button on the paste row, and `disabled` on gated rows.
- [x] 2.3 Style the new controls in `src/components/ShortcutsList.module.css`: reset the button's user-agent defaults so a control looks exactly like today's chip, add `cursor: pointer`, a hover border change, a `:focus-visible` brand outline, and a `:disabled` treatment (dimmed, `cursor: default`, no hover) matching the status bar's pin. Verify: in the running app the chips are pixel-identical at rest, the focus ring is visible when tabbing, and a disabled row does not react to hover or click.

## 3. Wiring

- [x] 3.1 Give `src/components/EditorPane.tsx` a `useImperativeHandle` exposing `{ applyChord }` that forwards to the adapter ref, accepting the React 19 `ref` prop. Verify: `src/components/EditorPane.test.tsx` renders with a ref and asserts a call reaches the fake adapter's recorded chords.
- [x] 3.2 Change `src/components/MetaPanel.tsx` to take the keyboard-shortcuts reference as a `shortcuts` node prop and render it in the last accordion section, so the panel keeps no knowledge of the feature. Verify: `src/components/MetaPanel.test.tsx` passes a stub node and still asserts the section is present, collapsed by default, and rendered in every state.
- [x] 3.3 Wire `src/App.tsx`: hold the editor ref, add `applyShortcut(chord, target)` that calls the editor handle for `editor` rows and dispatches at `document` for `app` rows, compute `canEdit` (`mode === 'page' && page !== null`) and `canSearch` (`graph !== null`), and pass the composed `ShortcutsList` into `MetaPanel`. Verify: `src/App.test.tsx` covers the app row focusing the search input, editor rows disabled with no page open, and the search row disabled with no vault.

## 4. Verification

- [x] 4.1 Add the payload test: seed a page containing a bold run, select it, apply `Mod-b`, and assert the serialized Markdown loses the emphasis markers; apply it again and assert they return. Verify: the test fails if `applyChord` is changed to add the mark unconditionally.
- [x] 4.2 Extend the live-keymap drift guard so every row with `replayable !== false` names a chord the running editor actually binds at some point. Verify: the test fails when a row's chord is edited to an unbound one.
- [x] 4.3 Confirm the keystroke path is untouched: the only new listener is `focusin` on the editor mount root, the panel subscribes to no editor state, and `ShortcutsList` receives only a callback and two booleans. Verify: read the diff against the design's budget section and run the full suite with `npx vitest run`.
- [x] 4.4 Check the two CodeMirror-bound rows by hand, since jsdom has no CodeMirror: with the caret in a code block, click "Cancel code block" and confirm it turns back into a paragraph, and click "Exit code block" and confirm the caret leaves the block. Verify: both behave as the keyboard chords do, and clicking either with the caret in ordinary prose changes nothing.
- [x] 4.5 Run `npx oxlint --fix`, `npm run fmt`, `npx oxlint --deny-warnings --format=agent`, and `npm run build`. Verify: lint reports no warnings, the build succeeds, and `openspec validate apply-shortcuts-on-click` still reports the change valid.

## 5. Records

- [x] 5.1 Write `adr/0016-apply-commands-by-key-chord.md` in the project's MADR format — Status: Accepted, Date, Context (the reference's rows now dispatch, ProseMirror keeps no command registry), Decision (replay the chord at the surface the caret is in), Consequences (toggle semantics come free, keydown-only reach, layout independence, no browser default action) — and add its row to the ADR table in `adr/README.md`. Verify: the README lists ADR-0016 and the file matches the format of the neighbouring records.
- [x] 5.2 At archive: replace the `actually apply keystroke when user click on it in shortkut window` line under PLAN.md's Later ideas with numbered task `21. [x] **…**`, carrying the shipped summary and the measured per-keystroke cost. Verify: PLAN.md has no remaining Later-ideas line for this work and task 21 is marked done.
