## 1. Measure the platform unknowns first

- [ ] 1.1 Browser check: with Folio installed as a PWA, Ctrl+Click a vault-linked PDF and record whether the bytes display in a new tab, open in the system browser, or fail. If it fails, amend the display clause of `page-editing` "Vault asset links open the stored file" (via `/opsx-update`) before writing any of section 2, and take the download branch in standalone mode.
- [x] 1.2 Browser check: in a scratch page, read a `.pdf` and a `.png` through the vault's binary read and log `file.type`. Record whether Chrome populates it; a result of `''` confirms the extension table in 2.1 is load bearing, not a nicety. **Done: `application/pdf` and `image/png` come back typed; only an extensionless name yields `''`.**
- [x] 1.3 Browser check: confirm the two gesture facts the design assumes — exactly one tab opens per Ctrl+Click, and `window.open` after the async read is not blocked. Record the result; if it is blocked, keep D4's pre-open and note it in a code comment. **Measured: one popup per click, with Ctrl held or not; `window.open` still opens after a 250 ms await.**
- [x] 1.4 Record all three results as a short "Facts" list at the top of `design.md`'s Context section, in the style `open-links-on-ctrl-click` used.

## 2. The target decisions module

- [x] 2.1 Add `src/editor/assetTarget.ts` with the pure decisions: a vault-relative target (no scheme, no leading `/`) decoded from its percent-encoded form to a vault path; a fragment and an external URL both yielding nothing; an extension (case-insensitive) mapped to a MIME type; and a "the browser displays this" set covering PDF, the image types the asset flow already recognises, common audio and video containers, and plain text. Verify with `src/editor/assetTarget.test.ts` covering: a plain relative path, a `%20` path, a path with a raw space, a leading `/`, `https:`, `data:`, `blob:`, `#fragment`, an unknown extension, and an uppercase extension.
- [x] 2.2 Add the two side-effecting openers in the same module — display the bytes in a tab (pre-opening the tab, assigning the object URL after the read, closing the blank tab when the read fails, revoking the URL on a timer) and deliver the bytes as a download — keeping each a few lines so the mix of DOM and logic stays testable apart. Verify by unit-testing the branch selection against a stubbed opener: displayable type calls the tab opener once, any other type calls the download opener once, and a failed read calls neither and closes the pre-opened tab.

## 3. Wire the gesture

- [x] 3.1 Extend the decoration plugin's Ctrl+Click branch in `src/editor/inlineDecorations.ts`: a vault-relative href goes to the reader and the openers instead of `openExternal`, still after `preventDefault`, with a plain click untouched. Verify with `src/editor/inlineDecorations.test.ts`: a Ctrl+Click on a vault-relative anchor reads once and opens; a Ctrl+Click with no vault reader reads nothing and opens nothing; an unresolvable path opens nothing and does not navigate; a plain click reads nothing; an external URL still takes `openExternal` and never the vault reader.
- [x] 3.2 Add the reader seam for the plugin: one setter on `EditorAdapter` and `MilkdownAdapter` beside `onReferenceClick`, read at activation time and answering `undefined` when no vault is open. Verify with the adapter's existing test double that the setter is what the plugin's click path calls, and that the reference-click path is unchanged.
- [x] 3.3 Attach the reader in `src/components/EditorPane.tsx` from the existing `readAssetRef` prop. Verified in the browser against an OPFS-backed vault: Ctrl+Click on a dropped PDF opened one window with `contentType: application/pdf`, and the page stayed open. Verify in the browser: Ctrl+Click a dropped PDF displays it and leaves the page open; `npm test` shows the existing pane tests still pass.

## 4. Prove the read budget and the vault is untouched

- [x] 4.1 Test the keystroke budget explicitly: with a vault reader that counts calls, type in a page that links vault files and assert zero reads, then activate a link and assert exactly one. Verified the test fails when a read is moved onto a document change. **Done at the plugin level (document transactions) and the pane level (an edit), which is where the read could regress; the app-level harness replaces the adapter that owns the click path.**
- [x] 4.2 Test that the gesture writes nothing: after activating a link, the insertion of the page's markdown, the saved file's bytes, and the storage double's write log are all unchanged. Verified at the plugin level (no transaction dispatched) and the pane level (no insertion, no change emitted, content and save stream untouched), plus the browser check's byte comparison of the vault file. **The app-level save harness cannot drive this gesture: it replaces the adapter that owns the click path.**

## 5. Record the boundary and finish

- [x] 5.1 Write `adr/0021-vault-files-open-as-derived-copies.md` recording the decision and its rejection of a native companion: a browser cannot hand a file on disk to the OS in place, so opening a vault file yields a copy with no write-back; add it to the `adr/README.md` index. Verify the index lists it and `openspec validate --changes open-vault-assets` still passes.
- [x] 5.2 Run `npx oxlint --fix`, then `npm run fmt`, then `npx oxlint --deny-warnings --format=agent`, `npm test`, and `npm run build`. Verified all five exit clean (666 tests, 38 files).
- [x] 5.3 Browser check on the real build, started with `npm run dev:test` (confirmed the log said `ready in` on port 58849, then swept with `npm run kill:dev`). Verified against an OPFS-backed vault: Ctrl+Click displayed the PDF in one window (`contentType: application/pdf`), downloaded the `.zip` as `archive.zip`, opened nothing for `#section`, opened `https://example.com/x` in a tab, and left the vault file's bytes unchanged.
- [x] 5.4 Bump `version` in `package.json` (minor: a new user-facing capability) and commit. Verified the header badge reads `v${version}` from `package.json` directly (`src/components/Header.tsx`), so the build names itself `v0.9.0`.
