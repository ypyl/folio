## 1. Dependencies and the table slice

- [x] 1.1 Add `@milkdown/preset-gfm`, `micromark-extension-gfm-table`, and `mdast-util-gfm-table` as direct dependencies in `package.json` (all three are already in the tree as transitive dependencies; pin them to the resolved versions in `package-lock.json`); verify `npm install` and `npx tsc -b` succeed and `node -e "require('./node_modules/@milkdown/preset-gfm/package.json').version"` prints the same version the lockfile holds.
- [x] 1.2 Create `src/editor/tableSetup.ts`: the `$remark` table-only plugin (D2) registering `gfmTable()`, `gfmTableFromMarkdown()`, and `gfmTableToMarkdown()`, and the slice Folio registers, built from named exports only (D1) — the five table schemas, `insertTableInputRule`, `tableKeymap`, `keepTableAlignPlugin`, `autoInsertSpanPlugin`, `tableEditingPlugin`, the navigation commands, the ten structural commands the component calls, and Folio's `$useKeymap` with `Mod-Alt-t`, `Mod-Alt-Enter`, and `Mod-Alt-Shift-Enter` (D5); verify `npx tsc -b` is clean and an `it` in `src/editor/milkdown.test.ts` asserts the editor's registered schema has a `table` node type while `strike_throughAttr` and the footnote types are absent from the context.
- [x] 1.3 Register the slice and `tableBlock` in `src/editor/milkdown.ts` after `.use(inlineDecorations(...))` (D8), supplying `tableBlockConfig.renderButton` with Folio markup for `add_row`, `add_col`, `delete_row`, `delete_col`, `align_col_left`, `align_col_center`, `align_col_right`, `col_drag_handle`, and `row_drag_handle`; verify `npx tsc -b` is clean and `npx vitest run src/editor/milkdown.test.ts` still passes.

## 2. Round-trip and behavior

- [x] 2.1 In `src/editor/milkdown.test.ts`, add a table group asserting: a pipe table seeds to a `table` node and serializes back as a pipe table; the canonical form is padded cells with a short delimiter row (`| a | b |` / `| - | - |`); the serialization is stable across a second parse and serialize; and column alignment written as `:--`, `:-:`, `---:` survives the round trip; verify `npx vitest run src/editor/milkdown.test.ts` passes.
- [x] 2.2 Add the no-leak assertions to the same file: for a page holding a bare `https://…`, a bare `www.…`, `~~struck~~`, `- [x] done`, and footnote syntax, the serialized Markdown is byte-identical to what the editor produces today, and no `link` mark, task-list `checked` attribute, `footnote_*` node, or `strike_through` mark exists in the document (design D2/Context); verify `npx vitest run src/editor/milkdown.test.ts` passes.
- [x] 2.3 Add the canonical-form edge cases: an empty cell serializes as `<br />` and is stable across reloads; a header-only table gains one empty body row; a paragraph line starting with `|` serializes with an escaped leading pipe while its text is unchanged (design D7, spec scenarios); verify `npx vitest run src/editor/milkdown.test.ts` passes.
- [x] 2.4 Add the keymap assertions through the adapter's `applyChord` (the path the shortcuts rows use): with the caret in a cell, `Tab` moves to the next cell, `Shift-Tab` to the previous, `Enter` leaves the table, `Mod-Alt-Enter` adds a row, `Mod-Alt-Shift-Enter` adds a column, and with the caret in a paragraph `Mod-Alt-t` inserts a table; verify each chord reports applied and the document changes as the spec's scenarios state.
- [x] 2.5 Add the reference-inside-a-cell assertion: with a table cell holding `#Inbox` and the caret in it, `Mod-Enter` activates the reference (the adapter's `onReferenceClick` listener receives `Inbox`) and leaves the table alone (design D8); verify `npx vitest run src/editor/milkdown.test.ts` passes.
- [x] 2.6 Extend `src/editor/documentTail.ts` so the appended paragraph appears when the last child is a `table` as well as a `code_block` (D6), keeping the existing tests green, and add an assertion that a page seeded with a trailing table holds a paragraph after it while the serialized Markdown ends with the table and no blank line; verify `npx vitest run src/editor/documentTail.test.ts src/editor/milkdown.test.ts` passes.
- [x] 2.7 Add the paste assertion: pasting Markdown table text into a page produces a `table` node rather than pipe paragraphs, through the same path a paste takes (design D3); verify `npx vitest run src/editor/milkdown.test.ts` passes.
- [x] 2.8 Add the gutter assertion: `getBlockLines()` over a page whose table sits between two paragraphs returns one line per top-level block, with the table's own first line as its entry, so the gutter numbers a table once (spec scenario); verify `npx vitest run src/editor/milkdown.test.ts` passes.

## 3. Table chrome and styling

- [x] 3.1 Style the table block's chrome in `src/components/EditorPane.module.css` from DESIGN.md's Tables rules — handles and controls hidden until the table is hovered or its selection is inside it, hairline row rules, muted uppercase header labels, no framed box, no tinted header bar, no vertical rules, and the alignment group quiet until used; verify by starting a browser check with `npm run dev:test`, opening a page with a table, and confirming the handles appear on hover, the header matches the Tables rules, and nothing shifts when the controls appear.
- [x] 3.2 Verify the controls' keyboard and accessibility shape in the same session: the `renderButton` markup carries an accessible name per control, a control is a real focusable button, and the new-row/new-column chords (2.4) work with the pointer never touching the table; verify the browser check shows each control's name in the accessibility tree and each chord's effect in the document.

## 4. Keyboard-shortcuts reference

- [x] 4.1 Add the six rows to `SHORTCUT_GROUPS` in `src/components/shortcuts.ts` — Insert table `Mod-Alt-t`, Add table row `Mod-Alt-Enter`, Add table column `Mod-Alt-Shift-Enter`, Next table cell `Tab`, Previous table cell `Shift-Tab`, Exit table `Enter` — placed with the editing rows and keeping `Tab`/`Shift-Tab`/`Enter` also listed under their existing text actions (ui-shell delta); verify `npx vitest run src/components/ShortcutsList.test.ts` passes with each row on one line at the panel's width.
- [x] 4.2 Extend `src/components/shortcuts.test.ts` so the drift guard covers the new rows, their chords, and that each is a replayable control (not a `replayable: false` row); verify `npx vitest run src/components/shortcuts.test.ts` passes.

## 5. ADR

- [x] 5.1 Write `adr/0017-adopt-gfm-table-slice-and-table-block.md` (MADR: Status Accepted, Date, Context, Decision, Consequences) recording the table slice plus the component table block, the whitelist over the `gfm` preset, the table-only remark plugin as the price of keeping bare URLs literal, the accepted canonical forms, and the rejected alternatives, and add its row to the index table in `adr/README.md`; verify both files name ADR-0017 consistently and the link resolves.

## 6. Gates

- [x] 6.1 Bump `version` in `package.json` by a minor increment (a new user-facing capability) and verify the header badge renders the new version in the running app.
- [x] 6.2 Run `npx oxlint --fix`, then `npm run fmt`, then `npx oxlint --deny-warnings --format=agent`; verify the last command is clean.
- [x] 6.3 Run `npm test` and `npm run build`; verify both pass, the coverage thresholds hold, and record the production bundle delta (the table block adds Vue-adjacent weight to a bundle that already carries Vue for the code block) in the change's notes.
- [x] 6.4 Browser-verify the end-to-end behavior with a real vault: open a page holding a hand-written table and confirm it renders as a table; edit a cell, add a row and a column, set a column's alignment, paste a Markdown table, and copy a table out and back; confirm the saved file is a pipe table in the canonical form with no rewrite of the page's bare URLs, `~~runs~~`, or `[x]` lines; reopen the page and confirm it shows the same table; then click in the space below a trailing table and confirm the typed text becomes a new block rather than going into a cell.
- [x] 6.5 Sweep the dev server with `npm run kill:dev` and verify no `vite` process is left running.
